# marlin_razer

This directory is reserved for the Marlin-RaZeR extension source used by the public inference integration.

The trimmed `gpt_fast_integration` code expects this extension under the module name `marlin_razer`.
